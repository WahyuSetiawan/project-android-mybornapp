<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apps.karama.mybornapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".kick_counter" />
        <activity android:name=".alaram" />
        <activity android:name=".Hpl" />
        <activity android:name=".konstraksi" />

        <receiver
            android:name=".AlarmReceiver"
            android:process=":remote" />

        <activity android:name=".SplashScreen" />
        <activity android:name=".MainMenu" />
        <activity android:name=".MasaSubur" />
        <activity android:name=".Settings" />
        <activity android:name=".tes" />
        <activity android:name=".DataKickActivity"></activity>
    </application>

</manifest>
